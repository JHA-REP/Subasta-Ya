/**
 * Configuración base del cliente API para comunicarse con SubastaYa.API.
 * Se implementará completamente en la etapa frontend.
 */

const BASE_URL = 'https://localhost:7001/api';

export const apiCliente = {
    base: BASE_URL,
    // Métodos de comunicación con la API se implementarán en etapas posteriores.
};

export default apiCliente;
